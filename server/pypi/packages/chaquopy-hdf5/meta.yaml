{% set version = "1.10.2" %}

package:
  name: chaquopy-hdf5
  version: {{ version }}

build:
  number: 2

source:
  url: https://github.com/hdfgroup/hdf5/archive/hdf5-{{ version | replace(".", "_") }}.tar.gz
