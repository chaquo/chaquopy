{% set version = "2.5.0" %}

package:
  name: tflite-runtime
  version: {{ version }}

build:
  number: 1

source:
  url: https://github.com/tensorflow/tensorflow/archive/v{{ version }}.tar.gz

requirements:
  host:
    - python
    - numpy 1.17.4
