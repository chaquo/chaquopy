{% set version = "1.16.1" %}

package:
  name: lsl
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/sccn/liblsl/releases/download/v{{ version }}/liblsl-{{ version }}-focal_amd64.deb

requirements:
  build:
    - cmake