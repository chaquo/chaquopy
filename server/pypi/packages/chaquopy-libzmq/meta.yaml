{% set version = "4.3.2" %}

package:
  name: chaquopy-libzmq
  version: {{ version }}  # See buildutils/bundle.py in pyzmq

build:
  number: 1

source:
  url: https://github.com/zeromq/libzmq/releases/download/v{{ version }}/zeromq-{{ version }}.tar.gz
