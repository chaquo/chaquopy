package:
  name: tensorflow
  version: 1.10.1

build:
  number: 7

source:
  url: https://github.com/tensorflow/tensorflow/archive/v1.10.1.tar.gz

requirements:
  # Everything is built for the host machine too: see README.md.
  build:
    - numpy 1.14.2
  host:
    - python
    - numpy 1.14.2
