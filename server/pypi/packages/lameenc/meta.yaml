{% set version = "1.4.2" %}

package:
  name: lameenc
  # When updating, consider updating chaquopy-lame as well.
  version: "{{ version }}"

source:
  url: https://github.com/chrisstaite/lameenc/archive/refs/tags/v"{{ version }}".tar.gz

requirements:
  host:
    - chaquopy-liblame 3.100
    - python
