# build-wheel.py requires Python 3.6 or later, and the requirements listed below.
#
# Python package builds are performed by launching the command `pipX.Y`, where `X.Y` is the
# target Python version. This command must be on the PATH, and launch a Python environment with
# the `setuptools` and `wheel` packages available.

dataclasses==0.6
jinja2==2.10
jsonschema==2.6.0
pyelftools==0.24
pyyaml==3.12
wheel==0.30.0
