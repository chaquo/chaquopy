# build-wheel.py requires Python 3.6 or later, and the requirements listed below.
#
# build-wheel.py performs Python package builds by launching a Python subprocess of the version
# specified by its --python argument. That version must have pip, setuptools and wheel
# available (these are all installed by default in a new virtualenv).

attrs==17.2.0
jinja2==2.10
jsonschema==2.6.0
pyelftools==0.24
pyyaml==3.12
wheel==0.30.0
