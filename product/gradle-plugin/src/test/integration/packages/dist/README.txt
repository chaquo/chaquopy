These packages were generated by running one or both of the following commands in the
packages/src subdirectories:

    python setup.py bdist_wheel --universal
    python setup.py sdist

Some of the packages were then duplicated or renamed to meet the needs of the tests.
