buildscript {
    version = file("../../../../../../../../VERSION.txt").text.trim()

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
        classpath files("../../../../../libs/gradle-${version}.jar")
        classpath "org.json:json:20160810"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
