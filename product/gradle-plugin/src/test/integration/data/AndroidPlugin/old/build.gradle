buildscript {
    version = file("${System.getenv('chaquopy_root')}/VERSION.txt").text.trim()

    repositories {
        jcenter()
        maven { url "${System.getenv('chaquopy_root')}/maven" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.3'
        classpath "com.chaquo.python:gradle:$version"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
