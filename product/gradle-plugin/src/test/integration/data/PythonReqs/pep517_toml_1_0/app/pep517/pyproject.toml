[build-system]
requires = ["setuptools", "wheel", "google-crc32c"]

# TOML 0.5: dotted keys (not supported by avakar/pytoml)
a.b = "hello"

# TOML 1.0: heterogenous values in arrays (not supported by uiri/toml)
c = ["world", 42]
